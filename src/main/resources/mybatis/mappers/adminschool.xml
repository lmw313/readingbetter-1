<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="adminschool">
	<select id="selectList" parameterType="string" resultType="schoolvo">
		<![CDATA[
			select * from school where title like '%' || #{kwd} || '%' order by no asc
		]]>
	</select>
	
	<insert id="insertSchool" parameterType="string">
		<![CDATA[
			insert into school values(seq_school.nextval, #{title})
		]]>
	</insert>
</mapper>