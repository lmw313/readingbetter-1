<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

	<select id="list" parameterType="membervo" resultType="membervo">
		<![CDATA[
			select no, id, name, penalty from member where id like '%' || #{kwd} || '%' order by no asc
		]]>
	</select>
	
	<select id="selectAuthUser" parameterType="membervo" resultType="membervo">
		<![CDATA[
			select no, name from member where id=#{id} and pw=#{pw}
		]]>
	</select>

	<delete id="delete" parameterType="membervo">
		<![CDATA[
			delete from member where no=#{no}
		]]>
	</delete>
	
</mapper>