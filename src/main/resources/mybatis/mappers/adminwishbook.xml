<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="adminwishbook">
	<select id="list" resultType="wishbookvo">
		<![CDATA[
			select w.no, w.title, m.NAME, w.recommend, w.accept from wishbook w, member m where w.MEMBER_NO = m.NO order by w.recommend desc
		]]>
	</select>
	
	<select id="view" parameterType="wishbookvo" resultType="wishbookvo">
		<![CDATA[
			select w.title, w.publisher, w.reason, w.reg_date as regDate, w.recommend, w.accept, m.name
				from wishbook w, member m 
				where w.MEMBER_NO = m.NO and w.no=#{no}
		]]>
	</select>
	
	<select id="findPublisher" parameterType="publishervo" resultType="publishervo">
		<![CDATA[
			select title from publisher where title like '%' || #{kwd} || '%' order by no asc
		]]>
	</select>
	
	<select id="findBook" parameterType="bookvo" resultType="bookvo">
		<![CDATA[
			select * from book where title like '%' || #{bkwd} || '%' order by no asc
		]]>
	</select>
	
	<select id="selectPublisher" parameterType="wishbookvo" resultType="publishervo">
		<![CDATA[
			select no, title from publisher where title=#{publisher}
		]]>
	</select>
	
	<insert id="insertPublisher" parameterType="wishbookvo">
		<![CDATA[
			insert into publisher values(seq_publisher.nextval, #{publisher})
		]]>
	</insert>
	
	<insert id="insertBook" parameterType="wishbookvo">
		<![CDATA[
			insert into book values(
				seq_book.nextval, 
				#{title}, 
				0, 
				'', 
				(select no from publisher where title=
					(select publisher from wishbook where no=#{no})), 
				null)
		]]>
	</insert>
	
	<update id="updateWishbookAcceptToOne" parameterType="wishbookvo">
		<![CDATA[
			update wishbook set accept=1 where no=#{no}
		]]>
	</update>
	
	<update id="updateWishbookAcceptToTwo" parameterType="wishbookvo">
		<![CDATA[
			update wishbook set accept=2 where no=#{no}
		]]>
	</update>
</mapper>